### TODO
- Config: uniformiser le styles et regle eslint + stylelint sur l'ensemble des fichiers (+ avoir une regle de tabulation et retour à la ligne commune pour l'ensemble des fichiers)
- Refactor: Voir le fonctionnement actuelle des données schema.org + ajouter et standardiser les données pour avoir toutes les données nécessaires pour un portfolio et/ou personne créant des projets (web, design, artistes, artisants...)
- Note : `getPrismicSitemapUrls` (shared/prismic-sitemap-urls.ts) n'est actuellement branché nulle part (pas de `server/routes/__sitemap__/urls.ts`, pas de `sitemap.urls` dans nuxt.config.ts) — code mort en l'état. Le brancher au module `@nuxtjs/sitemap` est une tâche de feature à part, pas traitée ici.

### Next step


### Done
- Refactor: uniformiser les fetchs des projets utilisés dans prismic-llms-projects.ts, prismic-sitemap-urls.ts — les deux faisaient un fetch identique (`createClient().getAllByType('project_page')`), factorisé dans `shared/prismic-projects.ts` (`getAllPrismicProjects`). `use-prismic-fetch-document-listing.ts` n'a volontairement pas été unifié avec ces deux fichiers : c'est un composable Vue réactif (usePrismic/useAsyncData, pagination/orderings par appelant), contexte d'exécution différent (client/SSR) des deux scripts Nitro/build-time — les unifier n'aurait fait que complexifier sans gain. Bugs trouvés et corrigés au passage : `ProjectsFeedSlice`/`VMainProjectListing` triaient sur des champs Prismic inexistants (`my.project.order_date`, `my.project_page.date` — seul `creation_date` existe réellement sur `project_page`) ; `ProjectsFeedSlice` utilisait `pageSize: 10` en pensant limiter le flux à 10 projets, alors que `pageSize` ne fait que fixer la taille de page HTTP pour `getAllByType` (qui pagine jusqu'à épuisement quel que soit le pageSize) — remplacé par `limit: 10`, le vrai paramètre de plafond. `prismic-llms-projects.ts` lisait aussi `doc.data.short_description`, un champ qui n'existe pas sur `project_page` (le vrai champ résumé est `excerpt`) — corrigé. `use-prismic-fetch-document-listing.ts` avait un bug distinct : la locale courante (`fetchLocaleOption`) n'était fusionnée que pour la clé de cache, jamais transmise à l'appel réel `getAllByType` — corrigé (`useProjectNeighbors`, lui, n'avait pas de bug malgré les apparences : `getAllByType` sans `limit` explicite récupère déjà tout, donc son appel sans options fonctionnait déjà correctement).
- Refactor: `VPictureSource` — comportement volontaire, pas un bug. C'est un composant "config seule" (`render: () => null`) : utilisé comme enfant du slot par défaut de `VImg`/`VPrismicImg`/`VPrismicMedia`, il ne rend jamais de DOM lui-même. `VImg` lit directement les props de ces vnodes via `useSlots()` pour construire les balises `<picture><source>` (art direction responsive). Même principe que les composants "headless" : le composant transporte de la config, pas du rendu. Voir le commentaire en tête de `app/components/VPictureSource.vue`.

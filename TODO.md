### TODO
- VHeaderHome: il faudrait qu'il ait une animation skeleton tant que la video en background n'as pas commencé
- VHeaderHome: lorsque VPrismicMedia display une video avec la props fit=cover, il faut que l'entiereté de la vidéo remplisse son element parent. Il faut que le rendu soit l'equivalent du object-fit: cover pour une image. il faut aussi prendre en compte le ratio de la video pour compenser les bandes horizontales.


- useNativeCarousel: le slide ne s'arrete pas vraiment à la derniere slide, 5px apres la derniere slide ca donne un effet de slide "fantome"
- integration: daire l'integration de la page projects listing
    - Ajouter un systeme de filtre par tag de projet, input radio qui filtre visuellement les projets affichés
- A11y:
    - VMainNav: Style visible pour les liens actif (aria-current)
    - VProjectCard: Style visible pour les liens actif (aria-current)
    - Checker les attributs des élements interactif, exemple: aria-expended
    - Nom accessible pour les liens
    - Hierarchie des headings


### Next step
- API: ce site est la nouvelle version de celui ci https://hugotomasi.com/, il faudrait dans un premier temps récupérer son contenu et de préparer un dossier sur mon ordi en local avec les fichiers correspondant (sur mon bureau). Aussi prévoir un plan de redirection. Une fois validé, il faudra faire un migration pour importer ces données dans le repo prismic.

- Note : `getPrismicSitemapUrls` (shared/prismic-sitemap-urls.ts) n'est actuellement branché nulle part (pas de `server/routes/__sitemap__/urls.ts`, pas de `sitemap.urls` dans nuxt.config.ts) — code mort en l'état. Le brancher au module `@nuxtjs/sitemap` est une tâche de feature à part, pas traitée ici.

### Done
- feat: ajouter un composant VAnimatedText qui utilise VSplitText (render=chars) lorsqu'il recoit un props active (trouver un nom plus pertinent) chaque lettre apparait (from translate-y: -100 vers translate-y: 0) chaque lettre a un delay de transition en fonction de son index. Mettre en place se composant pour les liens dans VMainNav pour que je puisse tester le fonctionnement.
- Integration: `VMarquee` créé (`app/components/VMarquee.vue`) — `MarqueeSlice` référençait déjà `<LazyVMarquee>` (space/speed/reverse props) sans que le composant n'existe. Boucle infinie en CSS pur : le contenu du slot par défaut est dupliqué une fois (deux `.group`), chaque `.group` réserve son propre gap de fin via `padding-right` (au lieu d'un gap entre les deux `.group`), donc `translateX(-50%)` sur le conteneur `.tape` (deux groupes identiques) boucle exactement sans à-coup. Anime en pause si `prefers-reduced-motion: reduce`. Rien à changer dans `MarqueeSlice/index.vue`, `LazyVMarquee` se résout maintenant automatiquement.
- Refactor: uniformiser les fetchs des projets utilisés dans prismic-llms-projects.ts, prismic-sitemap-urls.ts — les deux faisaient un fetch identique (`createClient().getAllByType('project_page')`), factorisé dans `shared/prismic-projects.ts` (`getAllPrismicProjects`). `use-prismic-fetch-document-listing.ts` n'a volontairement pas été unifié avec ces deux fichiers : c'est un composable Vue réactif (usePrismic/useAsyncData, pagination/orderings par appelant), contexte d'exécution différent (client/SSR) des deux scripts Nitro/build-time — les unifier n'aurait fait que complexifier sans gain. Bugs trouvés et corrigés au passage : `ProjectsFeedSlice`/`VMainProjectListing` triaient sur des champs Prismic inexistants (`my.project.order_date`, `my.project_page.date` — seul `creation_date` existe réellement sur `project_page`) ; `ProjectsFeedSlice` utilisait `pageSize: 10` en pensant limiter le flux à 10 projets, alors que `pageSize` ne fait que fixer la taille de page HTTP pour `getAllByType` (qui pagine jusqu'à épuisement quel que soit le pageSize) — remplacé par `limit: 10`, le vrai paramètre de plafond. `prismic-llms-projects.ts` lisait aussi `doc.data.short_description`, un champ qui n'existe pas sur `project_page` (le vrai champ résumé est `excerpt`) — corrigé. `use-prismic-fetch-document-listing.ts` avait un bug distinct : la locale courante (`fetchLocaleOption`) n'était fusionnée que pour la clé de cache, jamais transmise à l'appel réel `getAllByType` — corrigé (`useProjectNeighbors`, lui, n'avait pas de bug malgré les apparences : `getAllByType` sans `limit` explicite récupère déjà tout, donc son appel sans options fonctionnait déjà correctement).
- Refactor: `VPictureSource` — comportement volontaire, pas un bug. C'est un composant "config seule" (`render: () => null`) : utilisé comme enfant du slot par défaut de `VImg`/`VPrismicImg`/`VPrismicMedia`, il ne rend jamais de DOM lui-même. `VImg` lit directement les props de ces vnodes via `useSlots()` pour construire les balises `<picture><source>` (art direction responsive). Même principe que les composants "headless" : le composant transporte de la config, pas du rendu. Voir le commentaire en tête de `app/components/VPictureSource.vue`.
- Refactor: schema.org — `usePrismicMeta`/`usePrismicProjectSchemaOrg` étaient écrits mais jamais appelés (aucune page n'avait de title/meta/canonical, aucun CreativeWork par projet). Branchés sur les 4 pages. Champs fantômes corrigés (project_page.thumbnail/date/tag_group/awards/creative_work_type → main_media/creation_date, rien d'équivalent pour tag_group/awards). Setting réorganisé en tabs Website (en premier) + Publisher (publisher_name/description/url/email/socials, remplace site_name/email/socials) — rempli avec les vraies infos d'Hugo Tomasi. Person/WebSite consolidés dans un plugin unique (app/plugins/prismic-schema-org.ts). Bugs trouvés en marge : usePrismicSettingsDocument et usePrismicAboutDocument appelaient getSingle('settings'/'about') au lieu des vrais ids (setting/about_page) — le setting n'était jamais récupéré ni typé correctement ; projets/index.vue fetchait le mauvais type de document (HOME au lieu de PROJECT_LISTING).
- Bug: `pnpm build` échouait (`PromoteSlice/index.vue`, `color(black)`). Cause réelle : la fonction Sass maison `color()` (export Figma "variables2css", 45 variables `$colors-*`) n'était jamais importée nulle part — Sass la traitait silencieusement comme la fonction CSS native `color()` (Color-4), d'où l'échec. Audit complet : 41 des 45 variables `$colors-*` étaient du code mort (jamais référencées), seules 2 (`black`/`white`) servaient à 4 appels dans `VHeaderHome.vue`/`PromoteSlice/index.vue` — et `color()` résout à la compilation Sass, donc incompatible avec le vrai système de thème du projet (`--color-*`, runtime, via `VThemeSwitcher.vue`). Décision : supprimé entièrement (`functions/_color.scss`, `variables/_colors.scss`, `export/_colors.module.scss`, l'import mort dans `variables/_themes.scss`) plutôt que corrigé — les 4 appels remplacés par `var(--color-background)`/`var(--color-content)`/`color-mix()`, qui suivent maintenant le thème actif.
- Config: ESLint/Stylelint uniformisés. `pnpm lint:js` ne tournait plus du tout (typescript-eslint incompatible avec typescript 7.0.2) — fixé via un override pnpm-workspace.yaml (`typescript: ^5.9.3`, pas dans package.json qui garde son intention `^7.0.2`). Convention : tabs pour JS/TS/Vue (déjà la règle ESLint, ajout de `@stylistic/indent-binary-ops` qui entrait en conflit), 2-espaces pour CSS/SCSS (déjà 100% respecté, aucune règle stylelint d'indentation n'existe côté CSS — volontaire, pas un oubli). `.editorconfig` ajouté. `eslint.config.mjs`/`stylelint.config.mjs` ignorent maintenant `.agents/`/`.claude/`/`backup/` (skills tiers vendorisés, jamais à lint/fix). `--fix` appliqué sur tout le repo après correction des dernières erreurs manuelles (unused vars, ts-expect-error sans description, attribut de template inutile). Voir CLAUDE.md pour le détail de la convention.
